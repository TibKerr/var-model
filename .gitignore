# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets / local config (never commit the real key)
.env

# Test / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local databases / data artifacts
*.db
*.sqlite
*.sqlite3
# Root-level data cache only (not the src/var_model/data package).
/data/
